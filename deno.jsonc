{
   "imports": {
      "@/": "./src/"
   },
   // Might need to be set to true if I ever use npm modules run-time.
   // "nodeModulesDir": "none",
   // Might need to be set to true if I ever use npm modules run-time.
   // "lock": false,
   "tasks": {
      "e2e": "deno run --allow-read ./src/main.ts",
      "eslint": "deno run -A npm:eslint src"
   }
}